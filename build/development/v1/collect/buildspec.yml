version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.18
  build:
    commands:
      - echo Building the Go code...
      - cd handlers/v1/collect/post
      - go build main.go
artifacts:
  files:
    - '**/*'